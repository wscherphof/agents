# Source project repos cloned by the session-start hook.
/src/

*.log

# Runtime marker the session-start hook writes to name the step in progress
# (read back only to report which step failed).
.session-start-phase
